import { AccordionItem } from './accordion-item.directive';

describe('AccordionItem', () => {
  it('should create an instance', () => {
    const directive = new AccordionItem();
    expect(directive).toBeTruthy();
  });
});
